{
  "name": "medmcp-template",
  "build": {
    "dockerfile": "Dockerfile",
    "args": { "BASE_IMAGE": "medmcp-base:dev" }
  },
  "workspaceFolder": "/workspaces/medmcp-template",
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspaces/medmcp-template,type=bind",
  "mounts": [
    "source=${localEnv:XDG_RUNTIME_DIR}/docker.sock,target=/var/run/docker.sock,type=bind"
  ],
  "containerEnv": {
    "UV_LINK_MODE": "copy"
  },
  "postCreateCommand": "uv sync",
  "remoteUser": "root"
}
